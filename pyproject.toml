[project]
name = "platform-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]==0.121.3",
    "fastar==0.6.0",
    "mc-postgres-db==1.4.3",
    "pytest>=9.0.1",
    "pytest-asyncio>=1.3.0",
]
